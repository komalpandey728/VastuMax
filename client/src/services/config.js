// Toggle switch to choose database source
// true: Runs fully in the browser sandbox (LocalStorage + IndexedDB). Great for standalone testing.
// false: Connects to the Express + Mongoose server we built in the /server directory!
export const USE_LOCAL_STORAGE = true;
